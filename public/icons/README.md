# Icons Directory

This directory is structured to support individual icons with separate dark and light variants.
Each icon should have its own folder with `dark.svg` and `light.svg` files.

Example:
- `public/icons/react/dark.svg`
- `public/icons/react/light.svg`
